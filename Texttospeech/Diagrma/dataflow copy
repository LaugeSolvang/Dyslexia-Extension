@startuml
!theme plain

actor User
participant Popup as "UI"
participant Background as "background script"
participant Content as "content script"
database Dict as "dictionaires"

User -> Background : Opens Chrome
Background -> Dict : Fetch word data
Dict -> Background : Return word data
User -> Popup : Clicks 'Scramble Text!'
Popup -> Background : Transform text
Content -> Background : get dictionaries
Content -> Content : Scramble text using wordData
Content -> User : Display scrambled text

@enduml
